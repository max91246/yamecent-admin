# 本地開發 override（自動套用）
services:
  nginx:
    ports:
      - "8888:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./docker/nginx/sites-available-local:/etc/nginx/sites-available:ro

  mysql:
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql

  redis:
    ports:
      - "6380:6379"

volumes:
  mysql-data:
