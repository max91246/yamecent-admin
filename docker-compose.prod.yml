# 正式環境（GCP）：docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  nginx:
    ports:
      - "80:80"
      - "443:443"

  mysql:
    ports:
      - "3306:3306"
    volumes:
      - /home/max91246/.laradock/data/mysql:/var/lib/mysql

  redis:
    ports:
      - "6379:6379"
